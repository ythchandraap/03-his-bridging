import { Test, TestingModule } from '@nestjs/testing';
import { V0AttributeStatusController } from './v0.attribute.status.controller';
import { V0AttributeStatusServices } from './v0.attribute.status.service';

describe('RecipeController', () => {
  let controller: V0AttributeStatusController;

  // beforeEach(async () => {
  //   const module: TestingModule = await Test.createTestingModule({
  //     controllers: [V0AttributeStatusController],
  //     providers: [V0AttributeStatusServices],
  //   }).compile();

  //   controller = module.get<V0AttributeStatusController>(
  //     V0AttributeStatusController,
  //   );
  // });

  // it('should be defined', () => {
  //   expect(controller).toBeDefined();
  // });
});
